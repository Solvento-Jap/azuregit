{
	"name": "SQL script 862",
	"properties": {
		"content": {
			"query": "declare @table nvarchar(128)\ndeclare @idcol nvarchar(128)\ndeclare @sql nvarchar(max)\n--initialize those two values\nset @table = 'EDS.DI_PRODUCTGROUPING'\nset @idcol = 'PRODUCT_GROUP'\nset @sql = 'select ' + @idcol +' , (0'select @sql = @sql + ' + isnull(datalength(' + QUOTENAME(name) + '), 1)'from  sys.columns where object_id = object_id(@table) and   is_computed = 0 set @sql = @sql + ') as rowsize from ' + @table + ' order by rowsize desc'\nPRINT @sqlexec (@sql)\n\n\nselect * from sys.stats ",
			"metadata": {
				"language": "sql"
			},
			"currentConnection": {
				"databaseName": "testSQLpool",
				"poolName": "testSQLpool"
			},
			"resultLimit": 5000
		},
		"type": "SqlQuery"
	}
}